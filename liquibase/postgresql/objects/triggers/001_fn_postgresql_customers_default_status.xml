<?xml version="1.0" encoding="UTF-8"?>

<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
        http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="001_fn_postgresql_customers_default_status" author="tanisha">

        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="0">
                SELECT COUNT(*)
                FROM information_schema.routines
                WHERE specific_schema = 'public'
                  AND routine_name = 'fn_postgresql_customers_default_status'
                  AND routine_type = 'FUNCTION'
            </sqlCheck>
        </preConditions>

        <sql splitStatements="false" stripComments="false">
            CREATE FUNCTION public.fn_postgresql_customers_default_status()
            RETURNS TRIGGER
            LANGUAGE plpgsql
            AS $$
            BEGIN
                IF NEW.account_status IS NULL OR NEW.account_status = '' THEN
                    NEW.account_status := 'active';
                END IF;

                RETURN NEW;
            END;
            $$;
        </sql>

    </changeSet>

</databaseChangeLog>
