<?xml version="1.0" encoding="UTF-8"?>

<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
        http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="001_sp_postgresql_customer_order_summary" author="tanisha">

        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="0">
                SELECT COUNT(*)
                FROM information_schema.routines
                WHERE specific_schema = 'public'
                  AND routine_name = 'sp_postgresql_customer_order_summary'
                  AND routine_type = 'PROCEDURE'
            </sqlCheck>
        </preConditions>

        <sql splitStatements="false" stripComments="false">
            CREATE PROCEDURE public.sp_postgresql_customer_order_summary(
                IN customer_id_value VARCHAR,
                INOUT order_count INTEGER DEFAULT 0
            )
            LANGUAGE plpgsql
            AS $$
            BEGIN
                SELECT COUNT(*)
                INTO order_count
                FROM public.orders
                WHERE "Customer_ID" = customer_id_value;
            END;
            $$;
        </sql>

    </changeSet>

</databaseChangeLog>
