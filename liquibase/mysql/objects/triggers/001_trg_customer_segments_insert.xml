<?xml version="1.0" encoding="UTF-8"?>

<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
        http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="001_trg_customer_segments_insert" author="tanisha">

        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="0">
                SELECT COUNT(*) FROM information_schema.triggers
                WHERE trigger_schema = DATABASE()
                  AND trigger_name = 'trg_customer_segments_insert'
            </sqlCheck>
        </preConditions>

        <sql splitStatements="false" stripComments="false">
            CREATE TRIGGER trg_customer_segments_insert
            BEFORE INSERT ON customer_loyalty
            FOR EACH ROW
            BEGIN
                IF NEW.customer_id IS NULL OR NEW.customer_id = '' THEN
                    SET NEW.customer_id = CONCAT('CUST', LPAD(FLOOR(RAND() * 1000000), 6, '0'));
                END IF;
            END;
        </sql>

    </changeSet>

</databaseChangeLog>
